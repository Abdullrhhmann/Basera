# ============================================
# BASIRA REAL ESTATE - PRODUCTION ENVIRONMENT
# ============================================
# 
# IMPORTANT: Copy this file to .env and fill in your production values
# Never commit the .env file to version control
#

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV=production
PORT=5001

# Your production frontend URL (no trailing slash)
CLIENT_URL=https://your-frontend-domain.com

# Your production backend URL (no trailing slash)
SITE_URL=https://api.your-domain.com

# Rate limiting - requests per 15 minutes per IP
# Increase if you have high legitimate traffic
RATE_LIMIT_MAX=500

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# JWT_SECRET: CRITICAL - Must be a long, random, secure string
# Generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Minimum 64 characters recommended
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_STRING_MINIMUM_64_CHARACTERS

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string format:
# postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=public
#
# Replace with your production database credentials:
# - USERNAME: Your PostgreSQL user (e.g., basera_user)
# - PASSWORD: Your PostgreSQL password (strong password!)
# - HOST: Database host (localhost if on same server, or IP/domain if remote)
# - PORT: PostgreSQL port (default: 5432)
# - DATABASE: Your production database name (e.g., basera_prod)
#
DATABASE_URL=postgresql://basera_user:YOUR_STRONG_PASSWORD@localhost:5432/basera_prod?schema=public

# ============================================
# CLOUDINARY CONFIGURATION
# ============================================
# Get these from your Cloudinary dashboard: https://cloudinary.com/console
# Required for image and video uploads
#
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================
# AI ASSISTANT (OPTIONAL)
# ============================================
# Only required if using AI chat features
# Get API key from: https://openrouter.ai/
#
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# ============================================
# EMAIL CONFIGURATION (OPTIONAL)
# ============================================
# Only required if using email features (inquiries, notifications)
# Configure based on your email provider (Gmail, SendGrid, etc.)
#
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=noreply@your-domain.com

# ============================================
# MONITORING & LOGGING (OPTIONAL)
# ============================================
# LOG_LEVEL=info
# SENTRY_DSN=your_sentry_dsn_for_error_tracking

# ============================================
# PRODUCTION CHECKLIST
# ============================================
# Before deploying, ensure:
# ✓ All REPLACE_WITH_* values are updated with real credentials
# ✓ JWT_SECRET is a long, random string (64+ characters)
# ✓ Database credentials are correct and tested
# ✓ Cloudinary credentials are valid
# ✓ CLIENT_URL matches your frontend domain
# ✓ SITE_URL matches your backend domain
# ✓ PostgreSQL database exists and migrations are run
# ✓ Firewall allows only necessary ports (80, 443, 22)
# ✓ SSL certificate is installed (Let's Encrypt)
# ✓ File permissions are secure (chmod 600 .env)
# ✓ Regular database backups are scheduled

